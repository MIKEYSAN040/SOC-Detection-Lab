/*
Detection: Suspicious Rundll32 Advpack RegisterOCX Abuse

Data Source:

The EVTX attack sample was imported into Splunk
using the "detections" index.

EventCode=1:

Sysmon Event ID 1 records Process Creation events,
which are used to detect suspicious Rundll32 execution.
*/


/* Detection Query */

index=detections
Image="*\\rundll32.exe"
"advpack.dll"
"RegisterOCX"


/*
Validation Note:

The EVTX sample was indexed successfully. The Image,
ParentImage, ProcessId, and ParentProcessId fields
were extracted successfully.

The execution arguments were not exposed as a
dedicated field after ingestion. However, the raw
Sysmon event confirmed the presence of:

- advpack.dll
- RegisterOCX
- C:\Windows\System32\calc.exe
- Parent process: cmd.exe

These indicators validate the Sigma detection logic
and confirm abuse of Rundll32 (MITRE ATT&CK T1218.011).
*/


/* Investigation Query */

index=detections
Image="*\\rundll32.exe"
| table _time host ComputerName Image ParentImage ProcessId ParentProcessId